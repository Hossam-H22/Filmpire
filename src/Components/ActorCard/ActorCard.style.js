import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
    castImage: {
        width: '100%',
        maxWidth: '7em',
        height: '8em',
        objectFit: 'cover',
        borderRadius: '10px',
    },
}));